{
  "name": "@eoduksini/harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Repository-anchored software factory contracts and project adapters",
  "engines": {
    "node": ">=22.12.0"
  },
  "bin": {
    "eoduksini": "core/cli.mjs"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs tests/reused/node/*.test.mjs && vitest run --config vitest.reuse.config.mjs",
    "test:primitives": "node --test tests/reuse-integration.test.mjs tests/reused/node/*.test.mjs && vitest run --config vitest.reuse.config.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "studio": "node studio/server.mjs",
    "agent": "node agent/node-agent.mjs"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  },
  "devDependencies": {
    "vitest": "3.2.7"
  }
}
