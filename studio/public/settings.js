const $=id=>document.getElementById(id),roles=['head','planner','coder','reviewer','validator','general'],assignmentRoles=['planner','coder','reviewer','validator'];let connections=[],models=[],nodes=[],tasks=[],writable=false,currentProfile=null;
const post=async(path,payload)=>{const response=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json','X-Eoduksini-Request':'1'},body:JSON.stringify(payload)}),value=await response.json();
  if(!response.ok)throw new Error(value.code);return value};
const empty=text=>{const node=document.createElement('p');node.className='registry-empty';node.textContent=text;return node};
const bytes=value=>{const units=['B','KiB','MiB','GiB','TiB'];let amount=value,index=0;while(amount>=1024&&index<units.length-1){amount/=1024;index++}return `${amount.toFixed(index?1:0)} ${units[index]}`};
function connectionRow(value,catalog){const row=document.createElement('article'),copy=document.createElement('div'),name=document.createElement('strong'),meta=document.createElement('small'),status=document.createElement('span');
  name.textContent=value.display_name;meta.textContent=`${catalog.get(value.provider_id)??value.provider_id} · 비밀값 위치: CUSTOMER AGENT`;status.className='registry-status pending';status.textContent=value.status.replace('_',' ');
  copy.append(name,meta);row.append(copy,status);return row}
function modelRow(value){const row=document.createElement('article'),copy=document.createElement('div'),name=document.createElement('strong'),meta=document.createElement('small'),rolesNode=document.createElement('span');
  name.textContent=value.display_name;meta.textContent=value.provider_model_id;rolesNode.className='role-tags';rolesNode.textContent=value.role_capabilities.join(' · ');copy.append(name,meta);row.append(copy,rolesNode);return row}
function nodeRow(value){const row=document.createElement('article'),identity=document.createElement('div'),name=document.createElement('strong'),meta=document.createElement('small'),capacity=document.createElement('div'),status=document.createElement('span');
  name.textContent=value.display_name;meta.textContent=`${value.os} · ${value.arch} · Agent ${value.agent_version}`;capacity.className='node-capacity';capacity.textContent=`CPU ${value.cpu_logical} · RAM ${bytes(value.memory_bytes)} · GPU ${value.gpu_status}`;
  status.className=`registry-status ${value.connectivity==='online'?'online':'offline'}`;status.textContent=value.connectivity;identity.append(name,meta);row.append(identity,capacity,status);return row}
function refreshConnectionOptions(){const select=$('model-connection');select.replaceChildren();for(const value of connections){const option=document.createElement('option');option.value=value.connection_id;option.textContent=value.display_name;select.append(option)}
  $('model-form').querySelector('button').disabled=!writable||connections.length===0}
const addOption=(select,value,label)=>{const option=document.createElement('option');option.value=value;option.textContent=label;select.append(option)};
function compatibleNodes(modelId){const model=models.find(value=>value.model_id===modelId),connection=connections.find(value=>value.connection_id===model?.connection_id);
  return nodes.filter(value=>value.status==='active'&&value.adapters.includes(connection?.provider_id))}
function fillNodeOptions(role,selected=null){const select=$(`assignment-node-${role}`),modelId=$(`assignment-model-${role}`).value;select.replaceChildren();addOption(select,'','노드 선택');
  for(const node of compatibleNodes(modelId))addOption(select,node.node_id,node.display_name);if(selected&&[...select.options].some(value=>value.value===selected))select.value=selected}
function renderProfile(profile){currentProfile=profile;const heads=models.filter(value=>value.status==='active'&&(value.role_capabilities.includes('head')||value.role_capabilities.includes('general'))),head=$('head-model');
  head.replaceChildren();addOption(head,'','Head AI 선택');for(const model of heads)addOption(head,model.model_id,model.display_name);if(profile?.head_model_id)head.value=profile.head_model_id;
  $('profile-mode').value=profile?.mode??'automatic';for(const role of assignmentRoles){const modelSelect=$(`assignment-model-${role}`),assignment=profile?.assignments?.[role]??null;
    modelSelect.replaceChildren();addOption(modelSelect,'','자동/미지정');for(const model of models.filter(value=>value.status==='active'&&(value.role_capabilities.includes(role)||value.role_capabilities.includes('general'))))
      addOption(modelSelect,model.model_id,model.display_name);if(assignment)modelSelect.value=assignment.model_id;fillNodeOptions(role,assignment?.node_id)}
  $('profile-revision').textContent=profile?`R${profile.revision}`:'NEW';$('assignment-form').querySelector('button').disabled=!writable||!profile;
  const dispatchButton=$('dispatch-form')?.querySelector('button');if(dispatchButton)dispatchButton.disabled=!writable||!profile;toggleProfileMode()}
const named=(collection,id)=>collection.find(value=>value.model_id===id||value.node_id===id)?.display_name??id;
const reasonLabel={ROLE_CAPABILITY_EXACT:'역할 일치',GENERAL_CAPABILITY_FALLBACK:'범용 역할',NODE_ONLINE:'노드 온라인',ADAPTER_AVAILABLE:'Adapter 확인'};
function assignmentRow(role,value){const row=document.createElement('article'),title=document.createElement('strong'),model=document.createElement('span'),node=document.createElement('span'),source=document.createElement('small');
  title.textContent=role;model.textContent=value?named(models,value.model_id):'배정 없음';node.textContent=value?named(nodes,value.node_id):'수동 검토 필요';source.textContent=value?`${value.source==='automatic'?'자동':value.source==='manual'?'직접':'Head'} · ${value.reason_codes.map(code=>reasonLabel[code]??code).join(' · ')}`:'BLOCKED';
  if(!value){model.className='unassigned';node.className='unassigned'}row.append(title,model,node,source);return row}
function renderAssignment(value){const status=$('assignment-status');status.textContent=value.status==='READY'?'READY':'REVIEW';status.className=value.status==='READY'?'ready':'review';
  $('assignment-summary-title').textContent=value.status==='READY'?'현재 조건에서 배정 가능':'실행 전 수동 검토 필요';$('assignment-digest').textContent=value.decision_digest;
  const rows=[assignmentRow('head',value.head_assignment),...Object.entries(value.assignments).map(([role,item])=>assignmentRow(role,item))];$('assignment-results').replaceChildren(...rows);
  const issues=value.issues.map(item=>`${item.role}: ${item.code}`),limitLabel={COST_POLICY_NOT_CONFIGURED:'비용 최적화 정책 없음',PRIVACY_POLICY_NOT_CONFIGURED:'개인정보 정책 없음'},limits=value.optimization_limits.map(item=>`제한: ${limitLabel[item]??item}`);
  $('assignment-limits').textContent=[...issues,...limits,'권한: 실행·쓰기·승인·Git 게시·배포 모두 FALSE'].join(' · ');$('assignment-result').classList.remove('hidden')}
const shortDigest=value=>value?`${value.slice(0,10)}…${value.slice(-8)}`:'대기 중';
const statusLabel={AWAITING_APPROVAL:'승인 대기',WAITING_APPROVAL:'승인 대기',AWAITING_RECOVERY_APPROVAL:'복구 재승인 대기',WAITING_RECOVERY_APPROVAL:'복구 재승인 대기',QUEUED:'전달 대기',WAITING_DEPENDENCY:'선행 역할 대기',CLAIMED:'Agent 수신',RUNNING:'진행 중',SUCCEEDED:'완료',FAILED:'실패',BLOCKED:'차단',RECOVERY_REQUIRED:'복구 검토 필요'};
const statusClass=value=>['SUCCEEDED'].includes(value)?'success':['FAILED','BLOCKED'].includes(value)?'failed':value==='RECOVERY_REQUIRED'?'recovery':['QUEUED','CLAIMED','RUNNING'].includes(value)?'active':'waiting';
function dispatchRoleRow(item){const row=document.createElement('li'),index=document.createElement('span'),copy=document.createElement('div'),title=document.createElement('strong'),assignment=document.createElement('small'),state=document.createElement('span'),evidence=document.createElement('small');
  row.className=`dispatch-role ${statusClass(item.status)}`;index.className='dispatch-index';index.textContent=String(item.index+1).padStart(2,'0');title.textContent=item.role.toUpperCase();
  assignment.textContent=`모델 ${named(models,item.model_id)} · 노드 ${named(nodes,item.node_id)}`;state.className='dispatch-state';state.textContent=statusLabel[item.status]??item.status;
  evidence.className='dispatch-evidence';evidence.textContent=item.predecessor_dispatch_id?`선행 결과 ${shortDigest(item.predecessor_result_digest)} · 증거 ${shortDigest(item.predecessor_evidence_digest)}`:'시작 역할 · 작업 승인에 결속';
  if(item.recovery_reason)evidence.textContent=`${evidence.textContent} · ${item.recovery_reason}`;copy.append(title,assignment,evidence);row.append(index,copy,state);return row}
function dispatchCard(task){const article=document.createElement('article'),header=document.createElement('header'),copy=document.createElement('div'),eyebrow=document.createElement('p'),title=document.createElement('h3'),state=document.createElement('span'),objective=document.createElement('p'),facts=document.createElement('dl'),timeline=document.createElement('ol'),footer=document.createElement('footer');
  article.className=`dispatch-card ${statusClass(task.status)}`;eyebrow.className='eyebrow';eyebrow.textContent=`EPOCH ${task.dispatch_epoch} · PROFILE R${task.profile_revision}`;title.textContent=task.task_id;state.className='dispatch-status';state.textContent=statusLabel[task.status]??task.status;
  copy.append(eyebrow,title);header.append(copy,state);objective.className='dispatch-objective';objective.textContent=task.objective;
  for(const [label,value] of [['Task digest',shortDigest(task.task_digest)],['Graph digest',shortDigest(task.role_graph_digest)],['승인 소비',task.approval_consumed?'소비됨':'소비되지 않음']]){const dt=document.createElement('dt'),dd=document.createElement('dd');dt.textContent=label;dd.textContent=value;facts.append(dt,dd)}
  timeline.className='dispatch-timeline';timeline.setAttribute('aria-label',`${task.task_id} 역할 진행 순서`);timeline.replaceChildren(...task.dispatches.map(dispatchRoleRow));
  if(task.status==='AWAITING_APPROVAL'&&writable){const button=document.createElement('button');button.className='approval-action';button.type='button';button.textContent='이 작업 승인';button.addEventListener('click',()=>approveTask(task,button));footer.append(button)}
  if(['RECOVERY_REQUIRED','AWAITING_RECOVERY_APPROVAL'].includes(task.status)){const active=task.dispatches.find(item=>['RECOVERY_REQUIRED','WAITING_RECOVERY_APPROVAL'].includes(item.status)),
    recovery=[...(task.recoveries??[])].reverse().find(item=>item.dispatch_id===active?.dispatch_id);const panel=document.createElement('div');panel.className='recovery-controls',warning=document.createElement('p');warning.className='recovery-copy';
    warning.textContent=recovery?'종료 확인 증거가 기록됐습니다. 별도 복구 승인이 있어야 같은 역할을 새 epoch에서 재시도합니다.':'자동 재시도는 차단됐습니다. 이전 실행이 종료됐음을 확인한 증거의 SHA-256 digest를 입력하세요.';panel.append(warning);
    if(writable&&!recovery){const input=document.createElement('input'),button=document.createElement('button');input.type='text';input.maxLength=64;input.pattern='[0-9a-f]{64}';input.placeholder='64자리 evidence SHA-256';input.setAttribute('aria-label','종료 확인 증거 SHA-256');button.type='button';button.className='approval-action';button.textContent='증거 검수 기록';button.addEventListener('click',()=>assessRecovery(task,active,input,button));panel.append(input,button)}
    if(writable&&recovery){const evidence=document.createElement('small'),button=document.createElement('button');evidence.textContent=`Evidence ${shortDigest(recovery.evidence_digest)} · ${recovery.disposition}`;button.type='button';button.className='approval-action';button.textContent='복구 재시도 승인';button.addEventListener('click',()=>approveRecovery(task,recovery,button));panel.append(evidence,button)}footer.append(panel)}
  article.append(header,objective,facts,timeline);if(footer.childNodes.length)article.append(footer);return article}
function renderTasks(){const ordered=[...tasks].sort((a,b)=>b.created_at.localeCompare(a.created_at));$('dispatch-count').textContent=String(ordered.length);$('dispatch-tasks').replaceChildren(...(ordered.length?ordered.map(dispatchCard):[empty('아직 생성한 전달 작업이 없습니다.')]))}
async function reloadTasks(){const data=await fetch('/api/organization/tasks').then(value=>value.json());if(data.code)throw new Error(data.code);tasks=data.tasks??[];renderTasks()}
async function approveTask(task,button){button.disabled=true;$('dispatch-message').textContent='승인 결속을 확인하는 중입니다.';try{await post(`/api/organization/tasks/${encodeURIComponent(task.task_id)}/approve`,{expected_task_digest:task.task_digest,
    approval_id:`approval-${crypto.randomUUID()}`,ttl_ms:3_600_000,idempotency_key:`approve-${crypto.randomUUID()}`});$('dispatch-message').textContent='승인했습니다. Head Agent가 가져갈 때 승인 권한이 한 번만 소비됩니다.';await reloadTasks()}
  catch(error){button.disabled=false;$('dispatch-message').textContent=error.message==='TASK_DIGEST_MISMATCH'?'작업이 변경됐습니다. 목록을 새로 불러온 뒤 확인하세요.':'승인하지 못했습니다. Profile·epoch·작업 상태를 확인하세요.'}}
async function assessRecovery(task,dispatch,input,button){if(!/^[0-9a-f]{64}$/.test(input.value)){input.focus();$('dispatch-message').textContent='종료 확인 증거의 64자리 소문자 SHA-256 digest가 필요합니다.';return}button.disabled=true;
  $('dispatch-message').textContent='복구 증거를 현재 attempt와 epoch에 결속하는 중입니다.';try{await post(`/api/organization/tasks/${encodeURIComponent(task.task_id)}/recovery-assessments`,{attempt_id:dispatch.attempt_id,
    expected_task_digest:task.task_digest,recovery_id:`recovery-${crypto.randomUUID()}`,disposition:'RETRY_CONFIRMED_TERMINATED',evidence_digest:input.value,idempotency_key:`recover-assess-${crypto.randomUUID()}`});
    $('dispatch-message').textContent='증거 검수 기록을 보존했습니다. 아직 재시도 권한은 열리지 않았습니다.';await reloadTasks()}catch(error){button.disabled=false;$('dispatch-message').textContent=error.message==='TASK_DIGEST_MISMATCH'?'작업이 변경됐습니다. 새로고침 후 다시 검수하세요.':'복구 증거를 기록하지 못했습니다. attempt·epoch·증거를 확인하세요.'}}
async function approveRecovery(task,recovery,button){button.disabled=true;$('dispatch-message').textContent='복구 승인 경계를 확인하는 중입니다.';try{await post(`/api/organization/tasks/${encodeURIComponent(task.task_id)}/recovery-approvals`,{recovery_id:recovery.recovery_id,
    expected_recovery_digest:recovery.recovery_digest,approval_id:`recovery-approval-${crypto.randomUUID()}`,ttl_ms:3_600_000,idempotency_key:`recover-approve-${crypto.randomUUID()}`});
    $('dispatch-message').textContent='복구를 승인했습니다. 확인된 같은 역할 하나만 새 epoch에서 전달 대기합니다.';await reloadTasks()}catch{button.disabled=false;$('dispatch-message').textContent='복구 승인에 실패했습니다. 증거 digest·Profile·epoch를 다시 확인하세요.'}}
function toggleProfileMode(){const automatic=$('profile-mode').value==='automatic';for(const role of assignmentRoles){$(`assignment-model-${role}`).disabled=!writable||automatic;
  $(`assignment-node-${role}`).disabled=!writable||automatic}$('head-model').disabled=!writable;$('profile-form').querySelector('button').disabled=!writable||models.length===0}
async function loadProfile(){const data=await fetch('/api/organization/orchestration-profile').then(value=>value.json());renderProfile(data.profile)}
async function reload(catalog){const [providerData,modelData]=await Promise.all([fetch('/api/organization/providers').then(value=>value.json()),fetch('/api/organization/models').then(value=>value.json())]);
  connections=providerData.connections??[];models=modelData.models??[];$('connection-count').textContent=String(connections.length);$('model-count').textContent=String(models.length);
  $('connections').replaceChildren(...(connections.length?connections.map(value=>connectionRow(value,catalog)):[empty('아직 등록한 공급자가 없습니다.') ]));
  $('models').replaceChildren(...(models.length?models.map(modelRow):[empty('아직 등록한 모델이 없습니다.') ]));refreshConnectionOptions()}
async function reloadNodes(){const data=await fetch('/api/organization/nodes').then(value=>value.json());nodes=data.nodes??[];$('node-count').textContent=String(nodes.length);
  $('nodes').replaceChildren(...(nodes.length?nodes.map(nodeRow):[empty('등록된 고객 노드가 없습니다.')]))}
async function load(){try{const [session,catalogData]=await Promise.all([fetch('/api/session').then(value=>value.json()),fetch('/api/provider-catalog').then(value=>value.json())]);
  if(!session.authenticated){location.assign('/?access=LOGIN_REQUIRED');return}writable=['owner','admin'].includes(session.organization.role);
  $('organization').textContent=`${session.organization.name} · ${session.organization.role.toUpperCase()}${writable?'':' · 읽기 전용'}`;
  const catalog=new Map(catalogData.providers.map(value=>[value.id,value.name]));for(const value of catalogData.providers){const option=document.createElement('option');option.value=value.id;option.textContent=`${value.name} · ${value.placement}`;$('provider').append(option)}
  if(!writable)for(const form of document.querySelectorAll('form'))for(const control of form.elements)control.disabled=true;
  await Promise.all([reload(catalog),reloadNodes()]);await loadProfile();await reloadTasks();$('provider-form').addEventListener('submit',async event=>{event.preventDefault();try{await post('/api/organization/providers',{provider_id:$('provider').value,display_name:$('connection-name').value.trim()});
    $('connection-name').value='';$('provider-message').textContent='등록했습니다. Agent가 자격증명을 확인할 때까지 대기 상태입니다.';await reload(catalog)}catch(error){$('provider-message').textContent=error.message==='ORGANIZATION_ADMIN_REQUIRED'?'조직 관리자 권한이 필요합니다.':'공급자 정보를 등록하지 못했습니다.'}});
  $('model-form').addEventListener('submit',async event=>{event.preventDefault();const selected=[...document.querySelectorAll('input[name=role]:checked')].map(value=>value.value);if(selected.length===0){$('model-message').textContent='역할을 하나 이상 선택하세요.';return}
    try{await post('/api/organization/models',{connection_id:$('model-connection').value,provider_model_id:$('provider-model-id').value.trim(),display_name:$('model-name').value.trim(),role_capabilities:selected});
      $('provider-model-id').value='';$('model-name').value='';$('model-message').textContent='모델을 등록했습니다.';await reload(catalog);await loadProfile()}catch{$('model-message').textContent='모델 정보를 확인해 주세요.'}});
  $('enrollment-form').addEventListener('submit',async event=>{event.preventDefault();try{const value=await post('/api/organization/node-enrollments',{display_name:$('node-name').value.trim()});
    $('enrollment-token').textContent=value.token;$('enrollment-result').classList.remove('hidden');$('node-name').value='';$('node-message').textContent='등록 토큰을 발급했습니다.'}
    catch(error){$('node-message').textContent=error.message==='ORGANIZATION_ADMIN_REQUIRED'?'조직 관리자 권한이 필요합니다.':'등록 토큰을 발급하지 못했습니다.'}});
  $('copy-token').addEventListener('click',async()=>{try{await navigator.clipboard.writeText($('enrollment-token').textContent);$('node-message').textContent='토큰을 복사했습니다.'}
    catch{$('node-message').textContent='복사할 수 없습니다. 토큰을 직접 선택해 주세요.'}});$('profile-mode').addEventListener('change',toggleProfileMode);
  $('profile-form').addEventListener('submit',async event=>{event.preventDefault();const mode=$('profile-mode').value,assignments={};for(const role of assignmentRoles){const model_id=$(`assignment-model-${role}`).value,node_id=$(`assignment-node-${role}`).value;
    assignments[role]=mode==='automatic'||(!model_id&&!node_id)?null:{model_id,node_id}}try{const value=await post('/api/organization/orchestration-profile',{mode,head_model_id:$('head-model').value,assignments});
      renderProfile(value.profile);$('assignment-result').classList.add('hidden');$('assignment-status').textContent='NOT RUN';$('assignment-status').className='';$('profile-message').textContent='Orchestration Profile을 저장했습니다.'}catch{$('profile-message').textContent='모델 역할과 노드 Adapter 조합을 확인해 주세요.'}});
  $('assignment-form').addEventListener('submit',async event=>{event.preventDefault();const selected=[...document.querySelectorAll('input[name=assignment-role]:checked')].map(value=>value.value);
    if(selected.length===0){$('assignment-message').textContent='필요 역할을 하나 이상 선택하세요.';return}try{const value=await post('/api/organization/orchestration-assignment',{
      task_id:$('assignment-task-id').value.trim(),expected_profile_revision:currentProfile.revision,roles:selected});renderAssignment(value);
      $('assignment-message').textContent=value.status==='READY'?'배정 조건을 통과했습니다. 이 결과는 아직 실행 승인이 아닙니다.':'차단 사유를 해결한 뒤 다시 평가하세요.'}
    catch(error){$('assignment-result').classList.add('hidden');$('assignment-status').textContent='FAILED';$('assignment-status').className='review';
      $('assignment-message').textContent=error.message==='ORCHESTRATION_PROFILE_CHANGED'?'Profile이 변경됐습니다. 새로고침 후 다시 평가하세요.':'배정 조건과 Task ID를 확인해 주세요.'}});
  $('dispatch-form').addEventListener('submit',async event=>{event.preventDefault();const selected=[...document.querySelectorAll('input[name=dispatch-role]:checked')].map(value=>value.value);
    if(selected.length===0){$('dispatch-message').textContent='순차 역할을 하나 이상 선택하세요.';return}if(!currentProfile){$('dispatch-message').textContent='먼저 Orchestration Profile을 저장하세요.';return}
    const button=event.submitter;button.disabled=true;$('dispatch-message').textContent='현재 Profile과 노드 상태를 다시 검사하는 중입니다.';try{const value=await post('/api/organization/tasks',{task_id:$('dispatch-task-id').value.trim(),objective:$('dispatch-objective').value.trim(),
      profile_revision:currentProfile.revision,roles:selected,idempotency_key:`create-${crypto.randomUUID()}`});$('dispatch-message').textContent=`${value.task.task_id}을 생성했습니다. 내용을 확인한 뒤 별도로 승인하세요.`;
      $('dispatch-task-id').value='';$('dispatch-objective').value='';await reloadTasks()}catch(error){$('dispatch-message').textContent=error.message==='ORCHESTRATION_PROFILE_CHANGED'?'Profile이 변경됐습니다. 새로고침 후 다시 시도하세요.':'작업을 만들지 못했습니다. 역할 배정과 온라인 노드를 확인하세요.'}
    finally{button.disabled=!writable||!currentProfile}});$('dispatch-form').querySelector('button').disabled=!writable||!currentProfile;setInterval(()=>{reloadNodes().catch(()=>{});reloadTasks().catch(()=>{})},30_000)
}catch{$('provider-message').textContent='Registry를 불러오지 못했습니다.'}}
for(const role of roles){const label=document.createElement('label'),input=document.createElement('input'),text=document.createElement('span');input.type='checkbox';input.name='role';input.value=role;if(role==='general')input.checked=true;text.textContent=role;label.append(input,text);$('roles').append(label)}
for(const role of assignmentRoles){const row=document.createElement('div'),title=document.createElement('strong'),modelLabel=document.createElement('label'),modelSelect=document.createElement('select'),nodeLabel=document.createElement('label'),nodeSelect=document.createElement('select');
  row.className='assignment-row';title.textContent=role.toUpperCase();modelSelect.id=`assignment-model-${role}`;nodeSelect.id=`assignment-node-${role}`;modelLabel.append('모델',modelSelect);nodeLabel.append('노드',nodeSelect);
  modelSelect.addEventListener('change',()=>fillNodeOptions(role));row.append(title,modelLabel,nodeLabel);$('assignments').append(row)}load();
